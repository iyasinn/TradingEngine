namespace WinFormsLibrary1
{
    class MainForm : Form
    {
        //private readonly MessageQueue messageQueue;

        public MainForm()
        {

            // Initialize the main form UI components
        }

        // Implement the main form UI logic, event handlers, etc.

        protected override void Dispose(bool disposing)
        {
            // Cleanup resources

            base.Dispose(disposing);
        }
    }
}